## The brief

A tutoring business was running on Excel. Enrolments, teacher schedules, guardian follow-ups, and fee status all lived in spreadsheets that drifted apart weekly. Teachers, guardians, and admins each had their own workaround. Nothing shared a source of truth.

The brief: replace the spreadsheet with one platform and give each role a surface that fits how they actually work, on web, native, or both.

## The problem

<Callout label="ground truth">
Excel was the database and the CRM. Every enrolment, schedule change, and payment update meant someone editing a sheet by hand and hoping the other workbook matched.
</Callout>

The status quo:

- **Everything in sheets:** teacher assignments, guardian contacts, payment status, and attendance lived in separate workbooks.
- **Manual follow-ups:** reminders and updates depended on someone remembering to check a row and act on it.
- **Three audiences, zero shared workflow:** admins, teachers, and guardians each needed different tools, but the business only had spreadsheets and a group chat.

A single dashboard with role-conditional fields would have been faster to ship and miserable to use. So I built six surfaces around one API.

## The approach

The system is a [Turborepo](https://turbo.build) monorepo with six client surfaces and shared packages. Every app talks to the same Hono server through an [oRPC](https://orpc.unnoq.com) contract, so types flow end-to-end from the Drizzle schema to the React Native screen.

<Diagram caption="six surfaces, one schema. subdomain-scoped roles, shared contract, single Postgres">
  <svg viewBox="0 0 980 480" className="h-auto w-full text-line">
    <g fontFamily="var(--font-mono)" fontSize="14" fill="var(--fg-soft)" textAnchor="middle">
      {/* connecting edges (drawn first so nodes overlay them) */}
      {[
        { x: 150, y: 70, side: "top" },
        { x: 830, y: 70, side: "top" },
        { x: 150, y: 380, side: "bot" },
        { x: 490, y: 420, side: "bot" },
        { x: 830, y: 380, side: "bot" },
      ].map((a, i) => (
        <line
          key={i}
          x1={a.x}
          y1={a.side === "top" ? a.y + 25 : a.y - 25}
          x2="490"
          y2="220"
          stroke="currentColor"
          strokeWidth="0.8"
          opacity="0.45"
        />
      ))}

      {/* hub */}
      <circle cx="490" cy="220" r="90" fill="var(--accent-soft)" stroke="var(--accent)" strokeWidth="1.5" />
      <text x="490" y="215" fill="var(--accent)" fontSize="16">api.brighttutor</text>
      <text x="490" y="238" fontSize="13" opacity="0.85">hono · oRPC</text>

      {/* apps */}
      {[
        { x: 150, y: 70, label: "brighttutor.com", role: "marketing · web" },
        { x: 830, y: 70, label: "admin.brighttutor", role: "console · web" },
        { x: 150, y: 380, label: "teacher.brighttutor", role: "teacher · web" },
        { x: 490, y: 420, label: "expo apps", role: "teacher + guardian · native" },
        { x: 830, y: 380, label: "guardian.brighttutor", role: "guardian · web" },
      ].map((a, i) => (
        <g key={i}>
          <rect x={a.x - 110} y={a.y - 25} width="220" height="50" rx="4" fill="var(--bg-elev)" stroke="currentColor" />
          <text x={a.x} y={a.y - 5}>{a.label}</text>
          <text x={a.x} y={a.y + 14} fontSize="12" opacity="0.7">{a.role}</text>
        </g>
      ))}
    </g>
  </svg>
</Diagram>

<Callout label="shared">
Every app imports a tiny set of internal packages: <code>packages/db</code> (Drizzle schema), <code>packages/auth</code> (Better-Auth client), <code>packages/contracts</code> (the oRPC router type). One change, every app stays in sync.
</Callout>

## The hard parts

### Subdomain-routed roles

Teachers log in at `teacher.brighttutor.*`, guardians at the native app or `guardian.brighttutor.*`, admins at `admin.brighttutor.*`. Routing is at the subdomain, not at a `/teacher` path, so every screen, cookie, and error page belongs to the role. It's also a pleasant local DNS exercise; the README ships with the `/etc/hosts` lines for the dev loop.

<Callout label="why subdomains">
Two reasons. First, the URL becomes part of the role's identity. Guardians never see `/admin` in their browser bar and wonder. Second, cookies, CSP, and rate limits scope cleanly per subdomain without conditional middleware.
</Callout>

### End-to-end types across six surfaces

Every endpoint, error code, and payload flows through one oRPC router. The React Native app, the admin console, and the teacher web app all import the same typed client. When the schema changes in `packages/db`, every app that uses the affected route fails its type check in CI. There's no &ldquo;hopefully the mobile build picks it up&rdquo;.

<Stack
  items={[
    { name: "Turborepo", note: "Pipelines: shared db gens block app builds.", group: "infra" },
    { name: "Hono + oRPC", note: "One router, typed clients in every app.", group: "api" },
    { name: "Drizzle + Postgres", note: "Single schema, generated migrations checked in.", group: "data" },
    { name: "Better-Auth", note: "Role-scoped sessions across subdomains.", group: "auth" },
    { name: "Expo / React Native", note: "Teacher + guardian apps, same oRPC client, native UI.", group: "mobile" },
  ]}
/>

### One schema, three roles

Each audience gets a surface shaped for their job, not a shared dashboard with fields hidden:

- **Teachers:** schedule, attendance, and payouts on the web portal and the native app.
- **Guardians:** enrolment, progress, and tuition on the web portal and the native app.
- **Admins:** onboarding, payroll, and support from the console.

Enrolment, scheduling, and attendance write to the same Postgres tables regardless of which surface initiated the change. That replaces the spreadsheet: one source of truth, visible everywhere it needs to be.

## What's running today

<StatRow>
  <Stat value="6+" label="surfaces in the monorepo" />
  <Stat value="3" label="audience roles · subdomain-scoped" />
  <Stat value="1" label="oRPC contract end-to-end" />
  <Stat value="ts" label="100% typescript" />
</StatRow>

The marketing site is live at [brighteducations.com](https://brighteducations.com/). Role surfaces are in active deployment across the monorepo.

## What I'd do differently

I'd start with the oRPC contract before any UI. Half of the early friction came from web and native diverging on payload shapes before the server told them &ldquo;no, this is the shape&rdquo;. With the contract in place first, every app gets the same answer the same way.
